<main>
  <h1>Product list</h1>
  <div class="product-list">
    <% @products.each do |product| %>
      <button class="product"><%= link_to product.name, product_path(product.id) %></button>
    <% end %>
  </div>
</main>
